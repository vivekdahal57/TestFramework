/*
package com.vh.mi.automation.test.pages.analytics.claims;

import com.vh.mi.automation.api.comp.dataGrid.customizer.IDataGridCustomizer;
import com.vh.mi.automation.api.pages.analytics.claims.IClaims01;
import com.vh.mi.automation.impl.pages.analytics.claims.Claims01;
import com.vh.mi.automation.test.comp.dataGrid.DataGridCustomizationTest;
import org.testng.annotations.BeforeClass;

import static com.vh.mi.automation.api.constants.MILandingPages.CLAIMS_C01;
import static org.fest.assertions.Assertions.assertThat;

*/
/**
 * Created by i80448 on 11/20/2014.
 *//*

public class Claims01DataGridCustomizationTest extends DataGridCustomizationTest {
    private IClaims01 claims101;

    @BeforeClass
    public void setUp() {
        super.setUp();
        claims101 = navigationPanel.doNavigateTo(Claims01.class, defaultWaitTime);
        assertThat(navigationPanel.getCurrentPageTitle()).isEqualTo(CLAIMS_C01.getPageTitle());
    }


    @Override
    public IDataGridCustomizer getDataGridCustomizer() {
        return claims101.getDataGridCustomizer();
    }

    @Override
    public boolean isDataGridCustomizable() {
        return claims101.isDataGridCustomizable();
    }
}
*/
